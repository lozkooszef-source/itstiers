# ItsSubTiers

Separate Vercel frontend for the SubTiers-style rankings.

Use this folder as the Vercel project root directory and set:

```text
ITSTIERS_API_ROOT=https://itstiers.vercel.app/api/v2
```

The site exposes SubTiers-compatible endpoints under `/api/*` and maps them to the existing ItsTiers API.
